# Python
__pycache__/
*.pyc
.venv/
venv/

# PyInstaller build output (server/dist/ already covered by the Node
# "dist/" rule below, but its build/ intermediate dir isn't)
server/build/

# Node
node_modules/
dist/
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# Local data / secrets (the PAT itself lives in the OS keyring, never here)
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm
.env
*.log

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
